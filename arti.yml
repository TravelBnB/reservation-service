config:
  target: 'http://localhost:3003'
  phases:
    # - duration: 60
    #   arrivalRate: 30
    - duration: 60
      arrivalRate: 30
  variables:
    listingid:
      - [9999955, 9999981, 9999920, 9999725, 9999719, 9999889, 9999894]
    guestid:
      - [1, 2, 3, 4, 5]
scenarios:
  - flow:
      # - loop:
      #     - get:
      #         url: "/listings/{{ $loopElement }}/reservations"
      #   over: listingid
      - loop:
          - loop: 
              - get:
                  url: "/listings/{{ $loopElement }}/guests/{{ $loopElement }}/reservations"
            over: guestid
        over: listingid  
      # - get:
      #     url: "/listings/4020/reservations"
    # - loop:
    #   - get:
    #     url: "/listings/{{ $loopElement }}"
    #   over: listing_id
